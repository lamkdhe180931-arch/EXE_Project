{
  "name": "artdict",
  "version": "1.0.0",
  "description": "Minimalist Art Gallery & Showcase website",
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html, *.css, *.js\""
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "playwright": "^1.60.0"
  }
}
